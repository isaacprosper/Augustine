document.addEventListener('DOMContentLoaded', function() {
    let money = 190508020; // Start with R10,000,000,00
    const moneyDisplay = document.getElementById('money-display');
  
    setInterval(function() {
      money -= Math.floor(Math.random() * 1000); // Simulate money decrease
      moneyDisplay.textContent = `R${money.toLocaleString()}`; // Update the displayed money
    }, 1000); // Update every second
  });
  document.addEventListener('DOMContentLoaded', function() {
    let money = 1000; // Start with R10,000,000,00
    const moneyDisplay = document.getElementById('coin-btn-2');
  
    setInterval(function() {
      money += Math.floor(Math.random() *9.05); // Simulate money decrease
      moneyDisplay.textContent = `+${money.toLocaleString()}`; // Update the displayed money
    }, 800); // Update every 2second
  });
document.addEventListener('DOMContentLoaded', function() {
    let money = 111568305749; // Start with R10,000,000,00
    const moneyDisplay = document.getElementById('second-text');
  
    setInterval(function() {
      money -= Math.floor(Math.random() * 1000); // Simulate money decrease
      moneyDisplay.textContent = `R${money.toLocaleString()}`; // Update the displayed money
    }, 2000); // Update every second
  });
document.addEventListener('DOMContentLoaded', function() {
    let money = 413765181; // Start with R10,000,000,00
    const moneyDisplay = document.getElementById('third-text');
  
    setInterval(function() {
      money += Math.floor(Math.random() *10); // Simulate money decrease
      moneyDisplay.textContent = `R${money.toLocaleString()}`; // Update the displayed money
    }, 3000); // Update every second
  });
document.addEventListener('DOMContentLoaded', function() {
    let money = 650895985; // Start with R10,000,000,00
    const moneyDisplay = document.getElementById('text-fourth');
  
    setInterval(function() {
      money += Math.floor(Math.random() *10); // Simulate money decrease
      moneyDisplay.textContent = `R${money.toLocaleString()}`; // Update the displayed money
    }, 2000); // Update every 2second
  });
document.addEventListener('DOMContentLoaded', function() {
    let money = 559068985; // Start with R10,000,000,00
    const moneyDisplay = document.getElementById('text-fifth');
  
    setInterval(function() {
      money += Math.floor(Math.random() *10); // Simulate money decrease
      moneyDisplay.textContent = `R${money.toLocaleString()}`; // Update the displayed money
    }, 1000); // Update every 2second
  });
document.addEventListener('DOMContentLoaded', function() {
    let money = 2279; // Start with R10,000,000,00
    const moneyDisplay = document.getElementById('coin-btn');
  
    setInterval(function() {
      money += Math.floor(Math.random() *2.05); // Simulate money decrease
      moneyDisplay.textContent = `-${money.toLocaleString()}`; // Update the displayed money
    }, 800); // Update every 2second
  });
document.addEventListener('DOMContentLoaded', function() {
    let money = 1000; // Start with R10,000,000,00
    const moneyDisplay = document.getElementById('coin-btn-1');
  
    setInterval(function() {
      money += Math.floor(Math.random() *5.05); // Simulate money decrease
      moneyDisplay.textContent = `-${money.toLocaleString()}`; // Update the displayed money
    }, 800); // Update every 2second
  });
document.addEventListener('DOMContentLoaded', function() {
    let money = 1000; // Start with R10,000,000,00
    const moneyDisplay = document.getElementById('coin-btn-2');
  
    setInterval(function() {
      money += Math.floor(Math.random() *9.05); // Simulate money decrease
      moneyDisplay.textContent = `+${money.toLocaleString()}`; // Update the displayed money
    }, 800); // Update every 2second
  });
  document.addEventListener('DOMContentLoaded', function() {
    let money = 1020; // Start with R10,000,000,00
    const moneyDisplay = document.getElementById('coin-btn-3');
  
    setInterval(function() {
      money += Math.floor(Math.random() *9.05); // Simulate money decrease
      moneyDisplay.textContent = `+${money.toLocaleString()}`; // Update the displayed money
    }, 800); // Update every 2second
  });
  document.addEventListener('DOMContentLoaded', function() {
    let money = 8920; // Start with R10,000,000,00
    const moneyDisplay = document.getElementById('coin-btn-4');
  
    setInterval(function() {
      money += Math.floor(Math.random() *9.05); // Simulate money decrease
      moneyDisplay.textContent = `+${money.toLocaleString()}`; // Update the displayed money
    }, 800); // Update every 2second
  });